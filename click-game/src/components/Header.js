import React from 'react';

const Header = ()=> (
    <nav className={"navbar navbar-default navbar-fixed-top"}>
        <div className={"conatiner"}>
            Super Street Clicker II Turbo Alpha Extend
        </div>
    </nav>
);

export default Header;